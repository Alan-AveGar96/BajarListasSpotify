[phases.setup]
nixPkgs = ["python3Full", "ffmpeg", "nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["python3 -m pip install yt-dlp --break-system-packages"]

[start]
cmd = "node server.js"